class Poker(object):
    mydeck = Deck() #go to deck class
    total = 0
    mydeck.shuffle() #shuffles deck
    hand = mydeck.get_cards(2) #for first hand get two cards
    
    dealer = mydeck.get_cards(5) #for dealer the three cards to compare to
    
    def point(hand,dealer): #counts points of each hand
    values=?
    suits=?
    straight = (values == range(min(values), max(values)+1))
    
    def royal_flush(hand,dealer):
        
    def straight_flush(hand,dealer):
    
    def straight(hand,dealer):
        
    def flush(hand,dealer):
    
    def full_house(hand,dealer):
    
    def four(hand,dealer): #4 of a kind
    
    def three(hand,dealer):
    
    def two(hand, dealer):
        
    def one(hand,dealer):
        
    
        
        
    
